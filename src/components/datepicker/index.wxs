
module.exports = {
    setValue: function (newValue, oldValue, ownerInstance, instance) {
        console.log(newValue);
        if(newValue) {
            ownerInstance.callMethod('init', null);
        }
    }
}